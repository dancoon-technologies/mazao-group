/** Product lines on record-visit step 2 (string inputs until submit). */
export type VisitProductLine = {
  product_id: string;
  product_name: string;
  product_unit?: string;
  quantity_sold: string;
  quantity_given: string;
};
